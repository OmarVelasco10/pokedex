export type Pokemon = {
    name: string;
    id: string;
    img: string
}

export type PokemonDetails = {
    name: string;
    id: string;
    img: string
    hp: number;
    attack: number;
    defense: number
}